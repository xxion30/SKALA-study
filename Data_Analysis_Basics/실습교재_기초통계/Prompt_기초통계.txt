# 데이터 준비 

데이터 house_edu.csv 를 불러오는 파이썬 코드 보여줘

df 데이터 조회하는 코드 보여줘

df 데이터에서 id, zipcode 컬럼을 제거하는 코드 보여줘 

df 데이터 컬럼정보를 확인하기 위한 코드 보여줘

df 데이터에서 컬럼타입을 변경하려고 해, date 컬럼은 datetime 형으로, waterfront 는 범주형으로 변환하는 코드 보여줘


# 파생변수 생성

df 데이터에서 파생변수를 만들려고 해. 건축년도인 yr_built 와 주택판매일자인 date 를 이용해서 주택 나이 변수를 생성하는 코드 보여줘.
yr_built 는 float64 이고, date 는 datetime64[ns] 이야

df 데이터에서 living_area 컬럼을 bedrooms 컬럼으로 나누어 방밀도 변수를 생성하는 코드 보여줘


# EDA 

df 데이터에서 연속형 변수들의 분포를 수치로 확인할 수 있는 코드 보여줘

df 데이터에서 연속형 변수들의 빈도 분포를 시각적으로 확인할 수 있는 코드 보여줘

df 데이터에서 연속형 변수들의 박스 플롯 분포를 확인할 수 있는 코드 보여줘

df 데이터에서 연속형 변수들의 왜도, 첨도값을 확인할 수 있는 코드 보여줘

df 데이터에서 범주형 변수들의 분포를 수치로 확인할 수 있는 코드 보여줘, 건수와 비율을 같이 볼수 있게 해줘

df 데이터에서 범주형 변수들의 빈도 분포를 시각적으로 확인할 수 있는 코드 보여줘
구글 코랩에서 한글 깨짐현상이 발생하지 않는 코드도 같이 추가해줘

df 데이터에서 연속형 변수인 price 와 다른 연속형 변수와의 관계를 시각적으로 확인할 수 있는 코드 보여줘

df 데이터에서 연속형 변수인 price 와 다른 범주형 변수와의 관계를 시각적으로 확인할 수 있는 코드 보여줘


 
# 데이터 전처리

df 데이터에서 living_area 값이 (-) 인 경우 Missing 으로 대체하는 코드 보여줘


# Data Partition

df 데이터를 train 70% 과 test 30% 으로 분리하는 코드 보여줘 


# 원본 데이타 복제

df_train, df_test 데이터를 각각 df_train_raw, df_test_raw 데이터로 copy 하는 코드 보여줘


# Missing 처리

df_train 데이터에서 변수별 missing 건수 확인할 수 있는 코드 보여줘

df_train 데이터에서 연속형변수가 Missing 인 경우 중앙값으로 df_train과 df_test 를 대체하는 코드 보여줘

df_train 데이터와 df_test 데이터에서 범주형 변수가 Missing 인 경우 'Unknown' 으로 대체하는 코드 보여줘


# Outlier 처리

df_train 데이터에서 연속형변수의 경우 price 를 제외한 변수들을 |Z| > 3 기준으로 이상치 여부를 판단하고, 
이상치로 판단되는 값은 경계값으로 df_train과 df_test 를 대체하는 코드 보여줘
 

# Log 변환

df_train 데이터에서 연속형 변수 중 price를 제외한 변수들을 log 변환하고,
변환된 변수 + price 변수 + 범주형 변수를 모두 병합하는 코드를 보여줘.
단, log 변환 시 음수값이 존재할 수 있으므로 signed log transformation 으로 처리해줘.
그리고 df_test 데이터에서도 동일한 변환 기준을 적용하고
변환된 변수 + price 변수 + 범주형 변수를 모두 병합해줘.
결과 데이터는 동일한 이름(df_train, df_test)으로 유지해줘.


# 데이터 Scaling

df_train 데이터에서 연속형변수의 경우 price 를 제외한 변수들은 Min-Max Scaling 으로 표준화하고, 
표준화된 변수 + price 변수 + 범주형 변수를 모두 병합하는 코드 보여줘.
그리고 df_test 데이터에서도 동일한 표준화 기준을 적용하고 표준화된 변수 + price 변수 + 범주형 변수를 모두 병합해줘.
결과 데이터는 동일한 이름으로 해줘


# 상관분석

df_train 데이터에서 연속형 변수들 간의 상관성 분석과 히트맵 차트 생성하는 코드 보여줘


# 회귀분석 준비

df_train, df_test 데이터에서 price 를 종속변수로 하고 나머지는 설명변수로 해서 회귀분석을 하려고 해. 
종속변수 데이터와 설명변수 데이터를 분리하는 코드 보여줘

X_train 데이터에서 범주형 변수를 dummy 변수화 하고, dummy 변수와 연속형 변수를 병합해서 동일한 이름인 X_train 으로 생성해줘.
그리고 동일한 규칙을 X_test 데이터에도 적용하는 코드 보여줘

X_train 데이터의 컬럼타입을 확인하는 코드 보여줘

X_train, X_test 데이터에서 bool type 을 정수형 타입으로 변환하는 코드 보여줘


# 회귀분석

X_train, Y_train 데이터를 이용하여 회귀모델 생성하는 코드와 
X_test, Y_test 데이터를 이용하여 예측값을 생성하고, mape 로 평가하는 코드 보여줘, 
단, 모델 생성은 변수 유의성을 알수 있는 코드로 보여주고, mape 는 sklearn 을 이용해줘

df_test_raw 데이터에 y_pred 데이터 값을 병합하는 코드 보여줘

df_test_raw 데이타에서 연속형 변수 price 와 연속형 변수 y_pred 관계를 시각적으로 확인할 수 있는 코드 보여줘


# Baseline 모델 평가

Y_train 데이터에서 종속변수 평균을 계산해서 그 평균값을 예측값이라고 하고,
실측값 Y_test 와 mape 를 계산하는 코드 보여줘